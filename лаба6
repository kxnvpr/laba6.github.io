HTML
<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <title>Обробка рядка</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="container">
    <h2>Введіть будь-який текст:</h2>
    <textarea id="inputText" placeholder="Наприклад: a1b2c3!@#"></textarea>
    <button onclick="processString()">Обробити</button>
    <p id="result"></p>
  </div>

  <script src="sckr.js"></script>
</body>
</html>
CSS
body {
  font-family: Arial, sans-serif;
  background-color: #f5f5f5;
  margin: 0;
  padding: 20px;
}

.container {
  max-width: 600px;
  margin: 0 auto;
  background: white;
  padding: 20px;
  border-radius: 8px;
  box-shadow: 0 2px 6px rgba(0,0,0,0.1);
}

h2 {
  text-align: center;
  color: #333;
}

textarea {
  width: 100%;
  height: 100px;
  padding: 10px;
  margin: 10px 0;
  border: 1px solid #ccc;
  border-radius: 4px;
  font-size: 16px;
  box-sizing: border-box;
}

button {
  display: block;
  margin: 10px auto;
  padding: 10px 20px;
  background-color: #ac4caf;
  color: white;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  font-size: 16px;
}

button:hover {
  background-color: #a04545;
}

#result {
  margin-top: 20px;
  padding: 10px;
  background-color: #d01d8b;
  border-left: 4px solid #934caf;
  font-weight: bold;
  display: none;
}
JS
function processString() {
  const input = document.getElementById("inputText").value;
  
  if (!input) {
    alert("Будь ласка, введіть текст!");
    return;
  }

  let letters = "";
  let digits = "";

  for (let i = 0; i < input.length; i++) {
    const char = input[i];
    if (/[а-яА-Яa-zA-Z]/.test(char)) {
      letters += char;
    } else if (/[0-9]/.test(char)) {
      digits += char;
    }
  }

  const result = letters + digits;

  const resultElement = document.getElementById("result");
  resultElement.textContent = "Результат: " + result;
  resultElement.style.display = "block";
}
